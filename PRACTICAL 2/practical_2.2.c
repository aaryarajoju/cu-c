//PRACTICAL 2.2
#include <stdio.h>

int main(){

    int a, b;

    printf("Enter the values of a and b: ");
    scanf("%d %d",&a, &b);

    printf("Output of a&b is: %d\n", a&b);
    printf("Output of a|b is: %d\n", a|b);
    printf("Output of a^b is: %d\n", a^b);

    return 0;
}
